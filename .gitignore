# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Temp/debug artifacts
tmp_archive/
*_tmp.json
*_tmp.txt
tmp_*.txt
tmp_*.json
# cron scratch files (curl→python queue checks)
_*.json
_*.txt
_*.py
_*.md
.cron_prompts.tmp
# tmp/ is throwaway scratch for the headless cron jobs (curl dumps + one-off .py).
# Nothing in here is source; ignore the whole directory.
tmp/

# Local SIS data copies (real data is on Z:\01_SISデータ\PS\)
sisデータ/
tmpwork/
.tmpfb/

# 自動タスクが吐く一時ダンプ（リポジトリに入れない）
chat.json
fb_temp.json
posts*.json
